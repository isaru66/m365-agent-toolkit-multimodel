# Copy to backend.tf ONLY after creating storage with separate local bootstrap
# state; then migrate with a distinct bootstrap/state.tfstate key.
terraform {
  backend "azurerm" {
    use_azuread_auth = true
  }
}
